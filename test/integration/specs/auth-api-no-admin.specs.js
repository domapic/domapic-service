
const test = require('narval')

test.describe('auth api', () => {
  test.describe('when requested by no-admin users', () => {
    test.it('should return an error when trying to add a new api key', () => {
    })

    test.it('should return an error when trying to delete an existant api key', () => {
    })
  })
})
